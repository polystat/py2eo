[] > arithmetics-integer
  [args...] > unsupported
  [args...] > xunsupported
  memory > bogusForceDataize
  memory > xbogusForceDataize
  memory > xhack
  seq > @
    xhack.write
      []
        memory > xarithmetics_integer
        seq > @
          xarithmetics_integer.write
            []
              memory > xxorr
              memory > xmul
              memory > xuminus
              memory > xdiv
              memory > xrem
              memory > xshr
              memory > xorr
              memory > xuneg
              memory > xsub
              memory > xpow
              memory > xadd
              memory > xshl
              memory > xuplus
              memory > xandd
              seq > @
                seq
                  xpow.write ((((unsupported)).apply (10) (2)).eq 100)
                  xuminus.write ((1.neg).eq (0.sub 1))
                  xuplus.write (1.eq (1))
                  xuneg.write (((1.neg).and 7).eq 6)
                  xmul.write ((10.mul 10).eq 100)
                  xdiv.write ((((unsupported)).apply (100) (11)).eq 9)
                  xrem.write ((100.mod 11).eq 1)
                  xadd.write ((1.add 10).eq 11)
                  xsub.write ((10.sub 1).eq 9)
                  xshl.write ((((unsupported)).apply (1) (1)).eq 2)
                  xshr.write ((((unsupported)).apply (3) (1)).eq 1)
                  xandd.write ((13.and 6).eq 4)
                  xorr.write ((10.or 12).eq 14)
                  xxorr.write ((((unsupported)).apply (2) (3)).eq 1)
                  (((unsupported)).apply (((((((((((((((((xpow).not).or ((xuminus).not)).not).and (xuplus)).and (xuneg)).and (xmul)).and (xdiv)).and (xrem)).and (xadd)).and (xsub)).and (xshl)).and (xshr)).and (xandd)).and (xorr)).and (xxorr))))