enabled: True
python: |
  def divide(x, y):
    assert y != 0, 'Zero division'
    return round(x / y, 2)
  
  
  def check():
    res = False
    
    try:
      divide(21, 3)
      divide(21, 0)
    except AssertionError as e:
      res = True
    
    return res