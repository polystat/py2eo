disabled: True
python: |
  def augmented():
    x = 2
    x += 1
    plus = x == 3
    x -= 1
    minus = x == 2
    x *= 3
    mul = x == 6
    x //= 4
    div = x == 1
    x <<= 2
    shl = x == 4
    x %= 3
    mod = x == 1
    x |= 4
    orr = x == 5
    x &= 4
    andd = x == 4
    x >>= 1
    shr = x == 2
    x **= 4
    pow = x == 16
    x ^= 0b11000
    xorr = x == 8
    return plus and minus and mul and div and shl and mod and orr and andd and shr and pow and xorr
