python: |
  def myLi():
    class c:
        head = None
        tail = None
    
    def co(head, tail):
      o = c()
      o.head = head
      o.tail = tail
      return o
  
    lst = None
    lst = co(1, lst)
    lst = co(2, lst)
  
    return lst.tail.head == 1
